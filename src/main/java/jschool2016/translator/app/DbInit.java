package jschool2016.translator.app;

/*
@Component
public class DbInit {
    @Autowired
    PairService pairService;

    @PostConstruct
    @Transactional
    public void init(){
        Pair pair = new Pair();
        pair.setRuWord("мама");
        pair.setEnWord("mother");
        pair.setPriority(1);
        pairService.save(pair);

        pair = new Pair();
        pair.setRuWord("папа");
        pair.setEnWord("father");
        pair.setPriority(1);
        pairService.save(pair);

        pair = new Pair();
        pair.setRuWord("гусли");
        pair.setEnWord("gusli");
        pair.setPriority(10);
        pairService.save(pair);
    }
}
*/